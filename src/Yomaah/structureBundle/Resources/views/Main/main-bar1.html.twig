    <div id="main-bar">
        <div class="row">
            <ul class="menu" id="menu-left">
            {%for menu in menuleft%}
                <li><a href="{{path(menu.path)}}">{{menu.name}}</a></li>
            {%endfor%}
            </ul>
            <ul class="menu" id="menu-right">
            {%for menu in menuright  %}
                {%if (connect is defined and connect) or menu == false%}
                    <li><a href="{{path('post_log')}}">Se Déconnecter</a></li>
                {%else%}
                    <li><a href="{{path(menu.path)}}">{{menu.name}}</a></li>
                {%endif%}
            {%endfor%}
        </div>
    </div>
